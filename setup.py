from setuptools import setup

setup(
    name='kitty_watcher',
    version='0.1',
    py_modules=['kitty_watcher'],
    install_requires=[
    ],
    entry_points='''
        [console_scripts]
    ''',
)
